services:
  - type: web
    name: fastapi-app
    runtime: python
    buildCommand: ""
    startCommand: uvicorn app:app --host 0.0.0.0 --port 8089
    envVars:
      - key: PORT
        value: 10000
